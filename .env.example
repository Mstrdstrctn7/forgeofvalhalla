# Frontend config (Vite reads these at build time)
# Poll interval used ONLY when WebSocket fallback to HTTP is active.
# You can set this in Netlify → Site configuration → Environment variables too.
VITE_POLL_MS=3000
