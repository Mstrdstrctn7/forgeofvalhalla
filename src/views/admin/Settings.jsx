import React from "react";
import { Box, Heading, Text } from "@chakra-ui/react";

export default function Settings() {
  return (
    <Box>
      <Heading mb={4}>⚙️ Settings</Heading>
      <Text>Configure tipping, keys, and system modes.</Text>
    </Box>
  );
}
